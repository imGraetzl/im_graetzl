<% content_for :initial_content, flush: true do %>

  <%= render 'shared/stream/user_create_info', user: activity.trackable.user, activity: activity, entry: activity.trackable %>

  <%= render 'shared/stream/entry', entry: activity.trackable %>

<% end %>

<%= render 'comments/comment', comment: activity.trackable.comments.first if activity.trackable.comments.present? %>

