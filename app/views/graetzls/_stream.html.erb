<div id="stream-post-form">
  <%= render partial: 'posts/form', layout: 'shared/stream/entry_create' if user_signed_in? %>
</div>

<% @activities.each do |activity| %>
  <%= render_activity(activity, fallback: 'default', layout: 'stream/element') %>
<% end %>