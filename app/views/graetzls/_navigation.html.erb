<div class="navigation-bar-wrp">
  <div class="navigation-bar -small overthrow">

    <% if current_region.use_energieteiler? %>

      <%= active_link_to graetzl_path(@graetzl), active: :exact, class: "iconfilter" do %>
        <%= icon_tag "map-location" %>
        <div class="txt"><%= t("region.#{current_region.id}.graetzl") %> <br/><%= user_signed_in? ? "Stream" : "Übersicht" %></div>
      <% end %>

      <%= active_link_to rooms_graetzl_path(@graetzl), class: "iconfilter" do %>
        <%= icon_tag "raumteiler" %>
        <div class="txt">Raum<br/>teiler</div>
      <% end %>

      <%= active_link_to crowd_campaigns_graetzl_path(@graetzl), class: "iconfilter" do %>
        <%= icon_tag "crowd" %>
        <div class="txt">Crowd<br/>funding</div>
      <% end %>

      <%= active_link_to meetings_graetzl_path(@graetzl), class: "iconfilter" do %>
        <%= icon_tag "calendar-2" %>
        <div class="txt">Events & <br/>Workshops</div>
      <% end %>

      <%= active_link_to locations_graetzl_path(@graetzl), class: "iconfilter" do %>
        <%= icon_tag "location" %>
        <div class="txt">Anbieter &<br/>Macherinnen</div>
      <% end %>

      <%= active_link_to coop_demands_graetzl_path(@graetzl), class: "iconfilter" do %>
        <%= icon_tag "coop-and-share" %>
        <div class="txt">Markt <br/>platz</div>
      <% end %>

      <%= active_link_to energie_teiler_path, class: "iconfilter" do %>
        <%= icon_tag "sun" %>
        <div class="txt">Energie<br/>teiler</div>
      <% end %>

      <%= active_link_to zuckerls_graetzl_path(@graetzl), class: "iconfilter" do %>
        <%= icon_tag "shopping-bag" %>
        <div class="txt">Zuckerl &<br/>Angebote</div>
      <% end %>

    <% else %>

      <%= active_link_to graetzl_path(@graetzl), active: :exact, class: "iconfilter" do %>
        <%= icon_tag "map-location" %>
        <div class="txt"><%= t("region.#{current_region.id}.graetzl") %> <br/><%= user_signed_in? ? "Stream" : "Übersicht" %></div>
      <% end %>

      <%= active_link_to meetings_graetzl_path(@graetzl), class: "iconfilter" do %>
        <%= icon_tag "calendar-2" %>
        <div class="txt">Events & <br/>Workshops</div>
      <% end %>

      <%= active_link_to rooms_graetzl_path(@graetzl), class: "iconfilter" do %>
        <%= icon_tag "raumteiler" %>
        <div class="txt">Raum<br/>teiler</div>
      <% end %>

      <%= active_link_to crowd_campaigns_graetzl_path(@graetzl), class: "iconfilter" do %>
        <%= icon_tag "crowd" %>
        <div class="txt">Crowd<br/>funding</div>
      <% end %>

      <%= active_link_to locations_graetzl_path(@graetzl), class: "iconfilter" do %>
        <%= icon_tag "location" %>
        <div class="txt">Anbieter &<br/>Macherinnen</div>
      <% end %>

      <%= active_link_to coop_demands_graetzl_path(@graetzl), class: "iconfilter" do %>
        <%= icon_tag "coop-and-share" %>
        <div class="txt">Markt <br/>platz</div>
      <% end %>

      <%= active_link_to tools_graetzl_path(@graetzl), class: "iconfilter" do %>
        <%= icon_tag "tacker" %>
        <div class="txt">Geräte<br/>teiler</div>
      <% end %>

      <%= active_link_to zuckerls_graetzl_path(@graetzl), class: "iconfilter" do %>
        <%= icon_tag "shopping-bag" %>
        <div class="txt">Zuckerl &<br/>Angebote</div>
      <% end %>

    <% end %>

  </div>
</div>
