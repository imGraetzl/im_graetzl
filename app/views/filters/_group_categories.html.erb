<%= collection_check_boxes :filter, :group_category_ids, GroupCategory.all, :id, :title, { checked: params[:category] } do |b| %>
  <div class="input-checkbox filter-input">
    <%= b.check_box("data-label": b.object.title, "data-filter-label": 'Groups :: Category Modal') %>
    <%= b.label %>
  </div>
<% end %>
