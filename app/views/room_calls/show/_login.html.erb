<section class="submission-section -loggedout" id="call">

  <h2>Open Call - Deine Bewerbung</h2>

  <!--<div class="info">
    <div class="left">
      <h3 class="subtitle">Wer kann sich bewerben?</h3>
      <p class="description"><%= @room_call.about_partner %></p>
    </div>
    <div class="right">
      <h3 class="subtitle">Call Kontakt</h3>
      <div class="cardBox contact-infos">
        <% if @room_call.first_name.present? || @room_call.last_name.present? %>
          <div class="icontxt">
            <% if @room_call.user %>
              <%= attachment_image_tag(@room_call, :avatar, :fill, 200, 200, class: 'room_offer avatar img-round', fallback: 'avatar/user/200x200.png') %>
            <% else %>
              <%= icon_tag "user-avatar" %>
            <% end %>
            <div class="txt"><%= "#{@room_call.first_name} #{@room_call.last_name}" %></div>
          </div>
        <% end %>

        <% if @room_call.email.present? %>
          <%= mail_to @room_call.email do %>
            <div class="icontxt">
              <%= icon_tag "at-symbol" %>
              <div class="txt"><%= @room_call.email %></div>
            </div>
          <% end %>
        <% end %>

        <% if @room_call.website.present? %>
          <%= link_to @room_call.website, class: 'icontxt', target: '_blank' do %>
            <%= icon_tag "globe" %>
            <%= @room_call.website.sub(/^https?\:\/\//,'') %>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>-->

  <div class="steps">

    <div class="step">
      <div class="bigicontext-element -large">
        <%= icon_tag "mouse" %>
      </div>
      <h3>1. Kostenlos registrieren</h3>
      <p>Hole dir zuerst deinen kostenlosen imGrätzl Account und kehre eingeloggt auf diese Seite zurück.</p>
    </div>

    <div class="line">
      <%= image_tag('svg/round-line-up.svg', class: '-up') %>
    </div>

    <div class="step">
      <div class="bigicontext-element -large">
        <%= icon_tag "user-info-profile-details" %>
      </div>
      <h3>2. Call ausfüllen</h3>
      <p>Eingeloggt siehst du an dieser Stelle das Call Formular und du kannst dich bis zum <%= I18n.localize(@room_call.ends_at, format: '%d. %B') %> bewerben.</p>
    </div>

    <div class="line">
      <%= image_tag('svg/round-line-down.svg', class: '-down') %>
    </div>

    <div class="step">
      <div class="bigicontext-element -large">
        <%= icon_tag "speech-bubble" %>
      </div>
      <h3>3. Kennenlernen & Infos in der Gruppe</h3>
      <p>Wir schalten dich für die Raumteiler Gruppe frei, dort gibt es alle weiteren Infos.</p>
    </div>

  </div>

  <div class="loginpanel">
    <%= link_to 'Kostenlos registrieren & weiter zum Call', new_registration_path(redirect: request.original_url), class: 'btn-primary -rose -large -shadow' %>
  </div>


</section>
