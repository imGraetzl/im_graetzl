<section class="submission-section -loggedout" id="call">
  <h2>Open Call - Deine Bewerbung</h2>
  <h3 class="subtitle">Wer kann sich bewerben?</h3>
  <p><%= @room_call.about_partner %></p>

  <div class="steps">

    <div class="step">
      <div class="bigicontext-element -large">
        <%= icon_tag "mouse" %>
      </div>
      <h3>1. Einloggen</h3>
      <p>Log dich zuerst mit deinem Grätzl Account ein. Oder hol dir vorher einen Account.</p>
    </div>

    <div class="line">
      <%= image_tag('svg/round-line-up.svg', class: '-up') %>
    </div>

    <div class="step">
      <div class="bigicontext-element -large">
        <%= icon_tag "user-info-profile-details" %>
      </div>
      <h3>2. Call ausfüllen</h3>
      <p>Ein paar Infos über Dich und dein Vorhaben. Einreichfirst: <%= I18n.localize(@room_call.ends_at, format: '%d. %B') %></p>
    </div>

    <div class="line">
      <%= image_tag('svg/round-line-down.svg', class: '-down') %>
    </div>

    <div class="step">
      <div class="bigicontext-element -large">
        <%= icon_tag "speech-bubble" %>
      </div>
      <h3>3. Let's talk and meet</h3>
      <p>Nach Absenden des Calls landest du in der Hub-Gruppe und wir lernen uns alle kennen.</p>
    </div>

  </div>

  <div class="loginpanel">
    <%= link_to 'Mit deinem Account einloggen', new_user_session_path(redirect: request.original_url), class: 'btn-primary -mint' %>
    <span>oder</span>
    <%= link_to 'Neuen Account erstellen', new_registration_path(redirect: request.original_url), class: 'btn-primary' %>
  </div>


</section>
