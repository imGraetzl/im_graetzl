<% zuckerls = ActivitySample.new(graetzl: @user.graetzl).zuckerls %>

<% if zuckerls.any? %>

  <%= render 'summary/block_header', title: "Aktuelle Grätzlzuckerl im #{I18n.localize Time.now, format: '%B'} im #{@user.graetzl.district.numeric}. Bezirk" %>

  <% zuckerls.each do |zuckerl| %>
  <% location = zuckerl.location %>
  <% graetzl = Graetzl.memoized(location.graetzl_id) %>

  <!-- Zuckerl Card-->
  <table align="left" border="0" cellpadding="0" cellspacing="0" style="max-width:300px; margin-bottom:20px; border-bottom-width: 1px;border-bottom-style: dashed;border-bottom-color: #efefef;" width="100%" class="mcnTextContentContainer">
    <tbody>
      <tr>
        <td valign="top" class="mcnTextContent" style="padding-top:0; padding-left:18px; padding-bottom:18px; padding-right:18px;">
          <table>
            <tr>
              <td>
                <%= image_tag Notifications::ImageService.new.avatar_url(location), style: "width:70px; height:70px; border-radius: 4px; max-width:70px; min-width:70px; padding-right:10px;", class: "mcnImage" %>
              </td>
              <td style="font-size:12px; line-height:16px; vertical-valign:center;">
                <%= link_to location.name, graetzl_location_url(location.graetzl, location), style: "color:#ec776a;" %>
                <br>
                <%= location.location_category.name %>
                <% if location.address %>
                  <div class="address">
                    <%= "#{location.address.street_name} #{location.address.street_number.split(%r{/}).first}" %>
                  </div>
                <% end %>
              </td>
            </tr>
          </table>
          <%= link_to [graetzl, location, anchor: dom_id(zuckerl)] do %>
            <%= image_tag Notifications::ImageService.new.image_url(zuckerl), style: "width:100%; padding-bottom:10px; padding-top:10px;", class: "mcnImage" %>
          <% end %>
          <table>
            <tr>
              <td style="font-size:12px; line-height:25px; vertical-valign:center;"><img src="https://gallery.mailchimp.com/3accfa3feb5f05b82bd1fddc6/images/91f7f040-579d-43aa-968d-c19e08f100ef.png" style="width:20px; height:20px; padding-right:5px; float:left;" alt="" class="mcnImage">Grätzlzuckerl im <%= I18n.localize Time.now, format: '%B' %>:</td>
            </tr>
            <tr>
              <td>
                <%= link_to zuckerl.title, [graetzl, location, anchor: dom_id(zuckerl)], style: "color:#ec776a;" %>
              </td>
            </tr>
            <tr>
              <td><%= zuckerl.description %></td>
            </tr>
          </table>
        </td>
      </tr>
    </tbody>
  </table>
  <!-- /Zuckerl Card-->

  <% end %>

  <%= render 'mailers/shared/button', title: "Alle Grätzlzuckerl im #{I18n.localize Time.now, format: '%B'}", url: zuckerls_graetzl_url(@user.graetzl), style: "width:100%;" %>

<% end %>
