(function() {
  var $cardContainer = $("[data-behavior='toolteiler-card-container']");
  var $paginateLink = $("[data-behavior='toolteiler-paginate-link']");

  <% if params[:page].blank? %>
    $cardContainer.html("<%= j render @toolteiler %>")
  <% else %>
    $cardContainer.append("<%= j render @toolteiler %>");
  <% end %>

  <% if @groups.next_page.present? %>
    $paginateLink.attr('href', "<%= j next_page_url %>").removeClass("hide");
  <% else %>
    $paginateLink.addClass("hide");
  <% end %>
})();
