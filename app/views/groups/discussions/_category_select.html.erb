<div class="categories-list">
  <h3>Kategorien</h3>
  <ul>
    <li>
      <%= icon_tag('record') %>
      <%= link_to "Alle", group_discussions_path(@group), remote: true,
        class: params[:group_category_id].blank? ? 'autoload-link' : nil %>
    </li>
    <% @group.group_categories.each do |group_category| %>
      <li>
        <%= icon_tag('record') %>
        <%= link_to group_category.title, group_discussions_path(@group, group_category_id: group_category.id),
          remote: true, class: params[:group_category_id] == group_category.id.to_s ? 'autoload-link' : nil %>
      </li>
    <% end %>
  </ul>
</div>

<!-- for mobile we use a select menu -->
<div class="input-select categories-list-mobile">
  <label for="filter">
    <%= icon_tag "funnel-filter" %>
    <span>Diskussionen aus der Kategorie..</span>
  </label>
  <select id="filter"><!-- generate links from desktop view via JS --></select>
</div>
<!-- END mobile select menu -->
