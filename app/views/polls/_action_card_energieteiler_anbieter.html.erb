<% category_id = LocationCategory.where("name ILIKE :q", q: "%Energieteiler%").last&.id %>
<div class="cardbox-wrp" data-behavior="masonry-card">
  <div class="paperCard -cord1RT -illuteaser -poll">
    <div class="wrp">
      <div class="user">
        <% if current_user %>
          <%= avatar_image current_user, size: :thumb, class: 'avatar img-round show-badge' %>
          <span><%= current_user.username %></span>
        <% else %>
          <picture>
            <source srcset="<%= image_path('fallbacks/user_avatar.webp') %>" type="image/webp" />
            <%= image_tag('fallbacks/user_avatar.png', loading: 'lazy', class: 'img-round') %>
          </picture>
          <span>Dein Name</span>
        <% end %>
      </div>

        <h3>Du möchtest als Expert*in, Energiegemeinschaft oder Anbieter*in im Energieteiler aufscheinen?</h3>
        <%= link_to "Jetzt Schaufenster einrichten", new_location_path(location_category_id: category_id), class: 'btn-primary' %>
    </div>
    <div class="card"></div>
  </div>

  <% if local_assigns[:view_all] %>
    <%= link_to 'Zum Energieteiler', energie_teiler_path, class: 'link-more' %>
  <% end %>
</div>
