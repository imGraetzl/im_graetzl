<%- post = notification.activity.trackable %>

<%= content_tag(:li, id: dom_id(notification), data: { behavior: 'notification-item' }, class: 'notification-item') do %>
  <%= link_to post do %>
    <%= attachment_image_tag post.author, :avatar, :fill, 40, 40, fallback: 'avatar/user/40x40.png', class: 'img-round' %>
    <div class="msg">
      <div class="timeago">vor <%= time_ago_in_words(notification.activity.created_at) %></div>
      <span class="username"><%= post.author.username %></span> vom imGrätzl-Team hat einen <span class="matter">neuen Beitrag</span> geschrieben
    </div>
  <% end %>
<% end %>
