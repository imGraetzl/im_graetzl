<% meeting = notification.activity.trackable %>
<% graetzl = Graetzl.memoized(meeting.graetzl_id) %>

<%= content_tag(:li, id: dom_id(notification), data: { behavior: 'notification-item' }, class: 'notification-item') do %>
  <%= link_to [graetzl, meeting] do %>
    <%= image_tag avatar_url(notification.activity.owner, :thumb), class: 'img-round' %>
    <div class="msg">
      <div class="timeago">vor <%= time_ago_in_words(notification.activity.created_at) %></div>
      <span class="username"><%= notification.activity.owner.username %></span> hat ein neues Treffen im Grätzl <span class="matter"><%= graetzl.name %></span> erstellt
    </div>
  <% end %>
<% end %>
