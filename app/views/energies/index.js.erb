(function() {
  var $cardContainer = $("[data-behavior='energies-card-container']");
  var $paginateLink = $("[data-behavior='energies-paginate-link']");

  <% if params[:page].blank? %>
    $cardContainer.html("<%= j render @energies %>");
  <% else %>
    $cardContainer.append("<%= j render @energies %>");
  <% end %>

  <% if @next_page %>
    $paginateLink.attr('href', "<%= j next_page_url %>").removeClass("hide");
  <% else %>
    $paginateLink.addClass("hide");
  <% end %>
})();
