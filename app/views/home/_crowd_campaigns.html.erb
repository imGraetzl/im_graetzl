<div class="block-crowdcampaigns">
  <div class="hl-subtext">
    <h1>Lokales <span>Crowdfunding</span> auf <%= t("region.#{current_region.id}.domain_short") %></h1>
    <p>Setze deine Vorhaben mit der Kraft der Gemeinschaft um.</p>
  </div>
  <div class="cardBoxCollection -tripple -morelink">
    
    <% if current_region.is?('graz') || current_region.is?('innsbruck') %>
      <%= render @activity_sample.crowd_campaigns.first %>
      <%= render "shared/featured_cards/viertelfonds_#{current_region.id}" %>
    <% elsif current_region.is?('wien') %>
      <%= render @activity_sample.crowd_campaigns.first %>
      <%= render "shared/featured_cards/raumbooster_wien" %>
    <% else %>
      <%= render @activity_sample.crowd_campaigns.first(2) %>
    <% end %>

    <%= render 'crowd_campaigns/action_card', view_all: true %>
  </div>
</div>
