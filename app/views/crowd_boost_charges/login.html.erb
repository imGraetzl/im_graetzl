<% meta(
    title: t('.title'),
    robots: 'noindex, nofollow'
) %>

<section class="crowd-boost-charge-page login-screen">

  <div class="col-content">
    <section class="login">
      <%= render 'shared/login_panel_content', redirect: new_crowd_boost_crowd_boost_charge_url(@crowd_boost, amount:params[:amount]), origin: request.referrer %>
    </section>
  </div>

  <div class="col-sidebar">
    <%= render 'crowd_boost_charges/new/sidebar' %>
  </div>

</section>
