<% meta(
    title: t('.title'),
    robots: 'noindex, nofollow'
) %>

<section class="crowd-boost-charge-page login-screen">

  <div class="col-content">
    <section class="login">
      <%= render 'shared/login_panel_content', redirect: request.referrer, origin: request.referrer %>
    </section>
  </div>

  <div class="col-sidebar">
    <%= render @crowd_boost %>
  </div>

</section>
