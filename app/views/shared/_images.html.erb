<div class="entryImgUploads" data-featherlight-gallery data-featherlight-filter="a">
  <% imageable.images.each do |image| %>
    <%= link_to thumbnail_for(imageable, image), attachment_url(image, :file, :limit, 800, 800) %>
  <% end %>
</div>
