<div class="txt">
  <p><%= entry.content %></p>
</div>

<div class="entryImgUploads">
  <% entry.images.each do |image| %>
    <%= attachment_image_tag(image, :file, :fill, 400, 200) %>
  <% end %>
</div>