<div class="titleHeadline">
  <h3 class="district"><%= @graetzl.short_name %></h3>
  <h1><%= @meeting.name %></h1>
</div>

<div class="titleImg">
  <%= image_tag @meeting.cover_photo_url(:small) %>
</div>

<div class="entryDescription">

  <%= render 'time', object: @meeting %>

  <%= render 'location', object: @meeting %>  

  <p class="txt"><%= @meeting.description %></p>

</div>

<div class="entryParticipate">
  <%= render 'participation', object: @meeting %>
</div>

<div class="stream">
  <div class="writeEntry">
    Stream
  </div>
  <div class="streamEntry">

  </div>
</div>

<%= link_to 'Zurück', @graetzl %>