(function() {
  var $container = $("[data-behavior='participants-container']");

  function updateComponent() {
    $container.replaceWith("<%= j render 'meetings/participation', meeting: @meeting %>");
  }

  updateComponent();
})();
