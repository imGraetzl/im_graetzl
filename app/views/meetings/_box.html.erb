<div class="meetingBox">
  <div class="meetingBoxHeader">
    <%= link_to [meeting.graetzl, meeting] do %>
        <%= attachment_image_tag(meeting, :cover_photo,
                                 :fill, 300, 180,
                                 fallback: 'cover_photo/default.jpg',
                                 class: 'coverImg') %>
    <% end %>

    <% if meeting.starts_at_date %>
      <div class="calendarSheet">
        <div class="day"><%= meeting.starts_at_date.day %></div>
        <div class="month"><%= I18n.localize(meeting.starts_at_date, format:'%b') %></div>
      </div>
    <% end %>

    <% if meeting.location %>
      <div class="medalBadge">
        <%= attachment_image_tag(meeting.location, :avatar,
                                  :fill, 100, 100,
                                  fallback: 'avatar/small_default.png',
                                  class: 'img-round') %>
      </div>
    <% else %>
      <div class="sideflag -R"><%= meeting.graetzl.name %></div>
    <% end %>

    <% if initiator = meeting.initiator %>  
      <div class="user">
        <%= attachment_image_tag(initiator, :avatar,
                                  :fill, 100, 100,
                                  fallback: 'avatar/small_default.png',
                                  class: 'img-round') %>
        <span><%= initiator.username %></span>
      </div>
    <% end %>
  </div>
  <h3><%= meeting.name %></h3>

  <div class="txt">
    <%= simple_format(truncate(meeting.description, length: 140)) %>
  </div>
  <%= link_to 'Mitmachen', [meeting.graetzl, meeting], class: 'btn-primary -mint' %>
</div>