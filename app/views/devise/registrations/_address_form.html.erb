<div class="step-search" data-step="searchGraetzl">
  <div class="inner">

    <h5>Schritt 1/4 </h5>
    <h2>Lass uns zu Beginn dein <span>Heimatgrätzl</span> finden...</h2>

    <div class="assisted">
      <%= form_tag(fetch_graetzl_path, method: :post, remote: true) do %>

        <div class="input-group">
          <%= text_field_tag :address, nil %>
          <%= label_tag :address, 'An welcher Adresse in Wien wohnst du...?' %>
        </div>
        <div class="btn-navBar">
          <%= submit_tag 'Weiter', class: 'btn-primary next' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
