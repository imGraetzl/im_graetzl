(function() {
  <% if @comment.persisted? %>
    var $commentForm = $("[data-behavior='comment-form-<%= @location_post.id %>']");
    $commentForm.before("<%= j render @comment %>");
    $commentForm.replaceWith("<%= j render 'comment_form', location_post: @location_post %>");
    APP.components.stream.initCommentForm();
    APP.components.stream.initLinkify();
    APP.controllers.application.jBoxGallery();
  <% else %>
    alert('Es gab ein Problem, bitte versuche es später nochmal.');
  <% end %>
})();
