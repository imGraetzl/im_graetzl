<div class="mobileCreateContent" data-mobileNavID="createContent">
  <div class="createContainer">
    <%= link_to new_meeting_path(graetzl_id: current_user.graetzl_id) do %>
      <%= icon_tag "calendar-2" %>
      <div class="icontxt">Neues Treffen</div>
    <% end %>
    <%= link_to select_room_offers_path do %>
      <%= icon_tag "raumteiler" %>
      <div class="icontxt">Neuer Raumteiler</div>
    <% end %>
    <%= link_to new_tool_offer_path do %>
      <%= icon_tag "tacker" %>
      <div class="icontxt">Neuer Toolteiler</div>
    <% end %>
    <%= link_to new_location_path do %>
      <%= icon_tag "location" %>
      <div class="icontxt">Neue Location erstellen</div>
    <% end %>
    <% if current_user.admin? %>
      <%= link_to new_group_path(graetzl_id: current_user.graetzl_id) do %>
        <%= icon_tag "speech-bubble" %>
        <div class="icontxt">Neue Gruppe gründen</div>
      <% end %>
    <% end %>
  </div>
</div>

<div class="mobileNotifications nav-notifications" data-mobileNavID="notifications"></div>

  <div class="mobileSearch" data-mobileNavID="search">
    <div class="nav-search">
      <!-- Search Field-->
      <%= form_tag search_path, method: :get do |f| %>
        <%= hidden_field_tag :label, 'all' %>
        <%= hidden_field_tag :page, 1 %>
        <div class="input-field search-form">
          <%= text_field_tag :q, params[:q], placeholder: 'Suchbegriff ...' %>
        </div>
      <% end %>
      <!-- /Search Field-->
    </div>
  </div>

<div class="mobileUserSettings" data-mobileNavID="usersettings">
  <div class="nav-usersettings">

    <%= render 'nav_user_innerContent' %>

  </div>
</div>
