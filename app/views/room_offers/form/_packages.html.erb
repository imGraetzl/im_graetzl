<div class="form-block" id="tab-packages">

  <div class="hl-subtext">
    <h2>Raumteiler <span>Pakete</span></h2>
    <p>
      Hier kannst du mehrere Preispakete für eine längerfristige Vermietung definieren.
      Nutzer können eine Anfrage über die Platform stellen und ihr könnt euch kennenlernen.
    </p>
  </div>

  <div class="room-prices">
    <% f.object.room_offer_prices.build if f.object.room_offer_prices.empty? %>
    <%= f.fields_for :room_offer_prices do |price_form| %>
      <%= render 'room_offers/form/longterm_price_fields', f: price_form %>
    <% end %>
    <div class="room-prices-links">
      <%= link_to_add_association 'Weiteres Preispaket hinzufügen', f, :room_offer_prices, partial: 'room_offers/form/longterm_price_fields' %>
    </div>
  </div>

  <div class="input-button">
    <%= button_tag "Zurück", type: 'button', class: 'btn-secondary prev-screen', data: { tab: 'tab-contact' } %>
    <%= button_tag "Speichern", data: { disable_with: 'speichert...' }, class: 'btn-primary -saving', name: 'tab', value: 'packages' if !f.object.new_record? %>
    <%= button_tag "Weiter", type: 'button', class: "#{f.object.new_record? ? 'btn-primary' : 'btn-secondary'} next-screen", data: { tab: 'tab-slots' } %>
  </div>

</div>
