<div class="mapImgBlock">
  <div id="graetzlMapWidget" class="graetzlMapWidgetUseGraetzls" data-mapdata="<%= MapData.new.for_graetzl(Graetzl.in(current_region)).to_json %>"></div>
  <div class="activeArea"></div>
  <div class="navBlock">
    <h1><%= current_region %></h1>
    <div class="links">
      <% Graetzl.in(current_region).each do |graetzl| %>
        <%= link_to graetzl.name, graetzl %>
      <% end %>
    </div>
    <select class="mobileSelectMenu">
      <option disabled>Bezirk wählen</option>
    </select>
  </div>
</div>
