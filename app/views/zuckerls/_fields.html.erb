<%= form_errors_for f.object %>
<%= f.hidden_field :location_id %>

<div class="input-field">
  <%= f.label :title do %>
    <%= icon_tag "record" %>
    <span><%= Zuckerl.human_attribute_name :title %></span>
  <% end %>
  <%= f.text_field :title,
    placeholder: 'Gib deinem Angebot einen knackigen Titel ... (80 Zeichen)',
    id: 'zuckerl_title',
    maxlength: 80,
    data: { behavior: 'titleinput' } %>
</div>

<div class="input-textarea">
  <%= f.label :description do %>
    <%= icon_tag "layout-document" %>
    <span><%= Zuckerl.human_attribute_name :description %></span>
  <% end %>
  <%= f.text_area :description,
    placeholder: 'Beschreibe dein Angebot in 1-2 Sätzen ... (160 Zeichen)',
    id: 'zuckerl_description',
    maxlength: 160,
    data: { behavior: 'descriptioninput'} %>
</div>

<div class="input-field">
  <%= f.label :link do %>
    <%= icon_tag "globe" %>
    <span>Link zu</span>
  <% end %>
  <%= f.url_field :link,
    placeholder: 'http://www ...',
    id: 'zuckerl_link',
    data: { behavior: 'linkinput' } %><em></em>
</div>

<div class="upload-oldschool">
  <%= f.label :cover_photo do %>
    <%= icon_tag "photo-camera" %>
    <span>Bild hochladen</span>
  <% end %>
  <div class="field-wrp">
    <%= upload_image_input(f, :cover_photo) %>
  </div>
</div>

<div class="input-labledcheckbox">
  <%= f.label :entire_region do %>
    <%= icon_tag "map-location" %>
    <span>Zuckerl Sichtbarkeit</span>
  <% end %>
  <div class="field-wrp district_visibility">
    <div class="input-radio">
      <%= f.radio_button :entire_region, false, data: { behavior: 'entire_region_false', price: Zuckerl.price } %>
      <%= f.label "entire_region_false", "#{@location.districts.first.numeric}. Bezirk" %>
    </div>
    <div class="input-radio">
      <%= f.radio_button :entire_region, true, data: { behavior: 'entire_region_true', price: Zuckerl.region_price } %>
      <%= f.label "entire_region_true", "Ganz #{current_region}" %>
    </div>
    <div class="description">
      <small class="graetzl">
        Dein Zuckerl läuft im gesamten <%= @location.districts.first.numeric %>. Bezirk und kostet <span data-behavior="pricepreview"></span>.
      </small>
      <small class="entire_region">
        Dein Zuckerl läuft in ganz <%= current_region %> und kostet <span data-behavior="pricepreview"></span>.
      </small>
    </div>
  </div>
</div>
