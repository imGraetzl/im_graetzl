<%= div_for(post, class: 'streamElement') do %>
  <div class="entryInitialContent">
    <% if (current_user && post.edit_permission?(current_user)) %>
      <div class="editControls">
        <%= link_to post, { method: :delete, remote: true, data: { confirm: 'Beitrag wirklich löschen?', disable_with: 'lädt...' } } do %>
          <div class="btn-delete"><svg class="icon-trash-delete-remove"><use xlink:href="#icon-trash-delete-remove"></use></svg></div>
        <% end %>
      </div>
    <% end %>
    <div class="userCreateInfo">
      <%= link_to avatar_for(post.author), [post.author.graetzl, post.author] %>
      <span class="userAndType">Beitrag von <%= link_to post.author.username, [post.author.graetzl, post.author] %> <span>vor <%= time_ago_in_words(post.created_at) %></span></span>
    </div>
    <%= link_to [post.graetzl, post], class: 'linkToPost' do %>
      <div class="txt"><%= post.content %></div>
    <% end %>

    <%= render 'shared/images', imageable: post unless post.images.empty? %>
    
  </div>
  <%= render 'posts/comments', post: post %>
<% end %>
