<% title('Neue Location') %>

<section class="location-form">
  <div class="wrp">
    <h1 class="hl-main">Neue Location <%= t("region.#{current_region.id}.in_graetzl") %> <span><%= @location.graetzl.name %></span></h1>
  </div>

  <%= render 'locations/form/index' %>

</section>
