<div class="cardbox-wrp" data-behavior="masonry-card">
  <div class="paperCard -cord1RT -illuteaser -location">
    <div class="wrp">
      <div class="user">
        <% if current_user %>
          <%= avatar_image current_user, size: :thumb, class: 'avatar img-round' %>
          <span><%= current_user.username %></span>
        <% else %>
          <%= image_tag('fallbacks/user_avatar.png', class: 'img-round') %>
          <span>Dein Name</span>
        <% end %>
      </div>
      <% if local_assigns[:graetzl] %>
        <h3>Du bist unternehmerisch, sozial oder kreativ im Grätzl <%= graetzl.name %> tätig?</h3>
        <%= link_to 'Deine Location-Seite einrichten', new_location_path(graetzl_id: graetzl), class: 'btn-primary' %>
      <% elsif local_assigns[:district] %>
        <h3>Du bist unternehmerisch, sozial oder kreativ im <%= district.numeric %>. Bezirk tätig?</h3>
        <%= link_to 'Deine Location-Seite einrichten', new_location_path, class: 'btn-primary' %>
      <% else %>
        <h3>Über 7.500 Selbstständige, kl. Unternehmen & Macher*innen sind dabei.</h3>
        <%= link_to 'Deine Location-Seite gratis einrichten', new_location_path, class: 'btn-primary -rose' %>
      <% end %>
    </div>
    <div class="card"></div>
  </div>

  <% if local_assigns[:view_all] %>
    <% if local_assigns[:graetzl] %>
      <%= link_to 'Alle Anbieter & Locations', locations_graetzl_path(graetzl), class: 'link-more' %>
    <% elsif local_assigns[:district] %>
      <%= link_to 'Alle Anbieter & Locations', district_locations_path(district), class: 'link-more' %>
    <% else %>
      <%= link_to 'Alle Anbieter & Locations', region_locations_path, class: 'link-more' %>
    <% end %>
  <% end %>
</div>
