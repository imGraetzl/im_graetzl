<%= form_for Comment.new, url: comment_post_location_path(@location, post_id: post.id), remote: true,
    html: { class: 'entryForm', data: { behavior: "comment-form-#{post.id}" } } do |f| %>
  <label>
    <%= avatar_image current_user, size: :thumb, class: 'avatar img-round show-badge' %>
    <%= f.text_area :content, placeholder: 'Kommentieren' %>
  </label>
  <div class="hideBlock">
    <div class="upload-streamImgs">
      <%= icon_tag "photo-camera" %>
      <%= upload_image_input(f, :images, multiple: true, live_preview: false, max_files: 3) %>
    </div>
    <%= f.button 'Antwort senden', data: { disable_with: 'sendet...' } %>
  </div>
<% end %>
