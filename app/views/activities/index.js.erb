(function() {
  var $streamContainer = $("[data-behavior='stream-container']");
  var $paginateLink = $(".view-more-container");

  <% if params[:page].blank? %>
    <% if @activity.empty? %>
      $streamContainer.html("<%= j render 'empty_message' %>")
    <% else %>
      $streamContainer.html("<%= j render @decorated_activity %>")
    <% end %>
  <% else %>
    $streamContainer.append("<%= j render @activity %>");
  <% end %>

  <% if @activity.next_page.present? %>
    $paginateLink.html("<%= j next_page_link %>").show();
  <% else %>
    $paginateLink.hide();
  <% end %>

})();
