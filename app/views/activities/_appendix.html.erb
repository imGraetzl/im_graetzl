<% if comment = appendix[:comment] %>
  <div class="streamElement">
    <div class="entryInitialContent">
      <div class="userCreateInfo">
        <%= avatar_image_with_tooltip comment.user, size: :thumb, class: 'img-round' %>
        <span class="userAndType">Kommentar von <%= link_to comment.user.username, comment.user, rel: 'nofollow' %></span>
      </div>
      <div class="txt"><%= truncate comment.content, length: 100 %></div>
    </div>
    <% comments_count = parent.comments.count %>
    <% if comments_count > 1 %>
      <%= link_to "Alle #{comments_count} Kommentare ansehen", appendix_path_for(parent), class: 'commentslink' %>
    <% end %>
  </div>
<% end %>

<% if participant = appendix[:participant] %>
  <div class="userCreateInfo">
    <%= link_to participant, rel: 'nofollow' do %>
      <%= avatar_image_with_tooltip participant, size: :thumb, class: 'img-round' %>
    <% end %>
    <span class="userAndType"><%= link_to participant.username, participant, rel: 'nofollow' %> <span>nimmt teil.</span></span>
  </div>
<% end %>

<% if message = appendix[:message] %>
  <%= link_to [parent.graetzl, parent], class: "basic-message-box" do %>
    <div class="hl">
      <%= message[:title] %>
    </div>
    <div class="title">
      <%= message[:content] %>
    </div>
  <% end %>
<% end %>
