active_admin_form_for [:admin, resource] do |f|
  semantic_errors
  inputs do
    input :room_offer, collection: RoomOffer.joins(:group).uniq, include_blank: false, input_html: {class: 'admin-filter-select'}
    input :title
    input :description
    input :starts_at, default: Date.current
    input :ends_at, default: Date.current
    has_many :call_fields, allow_destroy: true, new_record: 'Field Hinzufügen' do |o|
      o.input :label
    end
  end

  actions
end
