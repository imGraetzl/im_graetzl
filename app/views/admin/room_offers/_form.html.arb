active_admin_form_for [:admin, resource] do |f|
  semantic_errors
  # inputs
  inputs do
    input :user, collection: User.all.sort_by(&:username), include_blank: false, input_html: { :class => 'admin-filter-select'}
    input :graetzl, collection: Graetzl.all.sort_by(&:name), include_blank: false, input_html: { :class => 'admin-filter-select'}
    input :district, collection: District.all.sort_by(&:name), include_blank: false, input_html: { :class => 'admin-filter-select'}
    input :location, collection: Location.all.sort_by(&:name), input_html: { :class => 'admin-filter-select'}
    input :slogan
    input :room_description
    input :total_area
    input :rented_area
    input :daily_rent
    input :longterm_rent
    input :owner_description
    input :tenant_description
    input :wants_collaboration
    input :slug
    input :offer_type
  end
  actions
end
