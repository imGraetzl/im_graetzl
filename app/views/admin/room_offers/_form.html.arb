active_admin_form_for [:admin, resource] do |f|
  semantic_errors
  # inputs
  inputs do
    input :user, collection: User.all.sort_by(&:username), include_blank: false
    input :graetzl, collection: Graetzl.all.sort_by(&:name), include_blank: false
    input :district, collection: District.all.sort_by(&:name), include_blank: false
    input :location, collection: Location.all.sort_by(&:name)
    input :slogan
    input :room_description
    input :total_area
    input :rented_area
    input :daily_rent
    input :longterm_rent
    input :owner_description
    input :tenant_description
    input :wants_collaboration
    input :slug
    input :offer_type
  end
  actions
end
