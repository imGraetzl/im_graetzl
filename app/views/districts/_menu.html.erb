<div class="filter-stream">

  <!-- for mobile we use a select menu -->
  <div class="input-select">
    <label for="filter">
      <%= icon_tag "funnel-filter" %>
      <span>Filter</span>
    </label>
    <select id="filter"><!-- generate links from desktop view via JS --></select>
  </div>
  <!-- END mobile select menu -->

  <%= active_link_to district_path(@district), class: "iconfilter" do %>
    <%= image_tag 'mapthumbs/stuwerviertel.jpg', class: 'mapthumb' %>
    <div class="txt"><%= @district.numeric %>. Bezirk <br/>Übersicht</div>
  <% end %>

  <%= active_link_to rooms_district_path(@district), class: "iconfilter" do %>
    <%= icon_tag "raumteiler" %>
    <div class="txt">Raum<br>teiler</div>
  <% end %>

  <%= active_link_to groups_district_path(@district), class: "iconfilter -coopshare" do %>
    <%= icon_tag "coop-share" %>
    <div class="txt">Gruppen</div>
  <% end %>

  <%= active_link_to locations_district_path(@district), class: "iconfilter" do %>
    <%= icon_tag "location" %>
    <div class="txt">Anbieter <br/>& Locations</div>
  <% end %>

  <%= active_link_to meetings_district_path(@district), class: "iconfilter" do %>
    <%= icon_tag "calendar-2" %>
    <div class="txt">Events & <br/>Workshops</div>
  <% end %>

  <%= active_link_to zuckerls_district_path(@district), class: "iconfilter" do %>
    <%= icon_tag "shopping-bag" %>
    <div class="txt">Zuckerl <br/>& Angebote</div>
  <% end %>

</div>
