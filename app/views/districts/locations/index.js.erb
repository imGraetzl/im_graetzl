(function() {
  var $streamContainer = $("[data-behavior='stream-container']");
  var $paginateLink = $("[data-behavior='paginate-link']");

  function updateComponent() {
    $streamContainer.append("<%= j render partial: 'locations/location', collection: @locations %>");
    $paginateLink.replaceWith("<%= j view_more_link @locations, style: :big %>");
    APP.components.masonryFilterGrid.adjustNewCards();
  }

  updateComponent();
})();
