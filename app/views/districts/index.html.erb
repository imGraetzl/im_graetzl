<% title('Wien') %>

<!--<section class="vienna" data-url="<%= districts_path %>">-->
<section class="vienna" data-map="<%= @map_data.to_json %>">

  <div class="mapImgBlock">
    <div id="graetzlMapWidget"></div>
    <div class="activeArea"></div>
    <div class="navBlock">
      <h1>Wien</h1>
      <div class="links">
        <% @districts.each do |district| %>
            <%= link_to "#{district.zip} - #{district.name}", district %>
        <% end %>
      </div>
      <select class="mobileSelectMenu">
        <option disabled>Bezirk wählen</option>
      </select>
    </div>
  </div>

  <% if @meetings.present? %>
      <div class="teaserBlock">
        <h2>Aktuelle Treffen in ganz Wien</h2>
        <div class="meetingBoxes">
          <% @meetings.each do |meeting| %>
              <%= render 'meetings/box', meeting: meeting %>
          <% end %>
        </div>
      </div>
  <% end %>

</section>