<% content_for :pagemap do %>
  <!--
  <PageMap>
    <DataObject type="document">
      <Attribute name="type">district</Attribute>
      <Attribute name="searchable">true</Attribute>
      <Attribute name="id"><%= @district.id %></Attribute>
      <Attribute name="title"><%= @district.name %></Attribute>
      <Attribute name="numeric"><%= @district.numeric %></Attribute>
      <Attribute name="zip"><%= @district.zip %></Attribute>
      <Attribute name="path"><%= district_path(@district) %></Attribute>
    </DataObject>
  </PageMap>
  -->
<% end %>
