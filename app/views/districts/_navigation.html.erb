<div class="navigation-bar-wrp">
  <div class="navigation-bar -small overthrow">

    <%= active_link_to district_index_path(@district), class: "iconfilter" do %>
      <%= icon_tag "map-location" %>
      <div class="txt"><%= @district.numeric %>. Bezirk <br/>Übersicht</div>
    <% end %>

    <%= active_link_to district_meetings_path(@district), class: "iconfilter" do %>
      <%= icon_tag "calendar-2" %>
      <div class="txt">Events & <br/>Workshops</div>
    <% end %>

    <%= active_link_to district_locations_path(@district), class: "iconfilter" do %>
      <%= icon_tag "location" %>
      <div class="txt">Anbieter &<br/>Macherinnen</div>
    <% end %>

    <%= active_link_to district_coop_demands_path(@district), class: "iconfilter" do %>
      <%= icon_tag "coop-and-share" %>
      <div class="txt">Markt <br/>platz</div>
    <% end %>

    <% if current_user&.beta_user? %>
      <%= active_link_to district_crowd_campaigns_path(@district), class: "iconfilter" do %>
        <%= icon_tag "crowd" %>
        <div class="txt">Crowd<br/>funding</div>
      <% end %>
    <% end %>

    <%= active_link_to district_rooms_path(@district), class: "iconfilter" do %>
      <%= icon_tag "raumteiler" %>
      <div class="txt">Raum<br/>teiler</div>
    <% end %>

    <%= active_link_to district_tools_path(@district), class: "iconfilter" do %>
      <%= icon_tag "tacker" %>
      <div class="txt">Tool<br/>teiler</div>
    <% end %>

    <%= active_link_to district_zuckerls_path(@district), class: "iconfilter" do %>
      <%= icon_tag "shopping-bag" %>
      <div class="txt">Zuckerl &<br/>Angebote</div>
    <% end %>

  </div>
</div>
