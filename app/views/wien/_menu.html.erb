<div class="filter-stream">

  <!-- for mobile we use a select menu -->
  <div class="input-select">
    <label for="filter">
      <svg class="icon-funnel-filter"><use xlink:href="#icon-funnel-filter"></use></svg>
      <span>Filter</span>
    </label>
    <select id="filter"><!-- generate links from desktop view via JS --></select>
  </div>
  <!-- END mobile select menu -->

  <%= active_link_to wien_path, class: "iconfilter" do %>
    <svg class="icon-map-location active"><use xlink:href="#icon-map-location"></use></svg>
    <div class="txt">Bezirks <br/>Übrsicht</div>
  <% end %>

  <%= active_link_to locations_wien_path, class: "iconfilter" do %>
    <svg class="icon-location"><use xlink:href="#icon-location"></use></svg>
    <div class="txt">Anbieter <br/>& Locations</div>
  <% end %>

  <%= active_link_to meetings_wien_path, class: "iconfilter" do %>
    <svg class="icon-calendar-2"><use xlink:href="#icon-calendar-2"></use></svg>
    <div class="txt">Events & <br/>Workshops</div>
  <% end %>

  <%= active_link_to rooms_wien_path, class: "iconfilter" do %>
    <svg class="icon-raumteiler"><use xlink:href="#icon-raumteiler"></use></svg>
    <div class="txt">Raum<br/>teiler</div>
  <% end %>

  <%= active_link_to zuckerls_wien_path, class: "iconfilter" do %>
    <svg class="icon-shopping-bag"><use xlink:href="#icon-shopping-bag"></use></svg>
    <div class="txt">Zuckerl <br/>& Angebote</div>
  <% end %>

  <% if user_signed_in? %>
    <!-- maybe add a create menu -->
  <% else %>
    <!--
    <%= link_to new_user_session_path, class: 'iconfilter loginlink' do %>
      <svg class="icon-login-signin"><use xlink:href="#icon-login-signin"></use></svg>
      <div class="txt">Registrieren<br/>& Login</div>
    <% end %>
    -->
  <% end %>
</div>
