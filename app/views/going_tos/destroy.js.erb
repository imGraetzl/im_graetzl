(function() {
  var $container = $("[data-behavior='participants-container']");

  function updateComponent() {
    $container.replaceWith("<%= j render 'going_tos/participation', meeting: @meeting %>");
  }

  updateComponent();
})();
