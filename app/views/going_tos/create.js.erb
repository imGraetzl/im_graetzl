console.log('attending');
if ( $('div.entryParticipate').length ) {

  $('div.entryParticipate').html("<%= j render 'meetings/participation', users: @meeting.users %>");

} else if ($("<%= j(div#meeting_#{meeting.id}) %>").length) {
  console.log("In else");

  $("<%= j('div#meeting_#{meeting.id} p') %>")
    .html('hello');

}
