(function() {
  var $cardContainer = $("[data-behavior='rooms-card-container']");
  var $paginateLink = $("[data-behavior='rooms-paginate-link']");

  <% if params[:page].blank? %>
    $cardContainer.html("<%= j render @rooms %>");
  <% else %>
    $cardContainer.append("<%= j render @rooms %>");
  <% end %>

  <% if @next_page %>
    $paginateLink.attr('href', "<%= j next_page_url %>").removeClass("hide");
  <% else %>
    $paginateLink.addClass("hide");
  <% end %>
})();
