<% title('Crowdfunding Kampagne bearbeiten') %>

<section class="crowdfunding-form">

  <%= render 'crowd_campaigns/form/navigation' %>
  <%= render 'crowd_campaigns/form/finish_video' %>

  <%= form_for(@crowd_campaign, html: { class: 'crowdfunding_form' }) do |f| %>
  <%= form_errors_for @crowd_campaign %>

  <%= render 'crowd_campaigns/form/finish', f: f %>

  <div class="input-button">
    <%= f.button 'Speichern', data: { disable_with: 'speichert...' }, class: 'btn-secondary -mint' %>
    <%= link_to 'Vorschau', 'javascript:', class: ["btn-secondary", "-mint", @crowd_campaign.editable? ? 'save-and-preview' : '-disabled'] %>
    <% if f.object.pending? %>
      <%= link_to 'Dein Projekt wird gerade geprüft', edit_next_steps_crowd_campaign_path(@crowd_campaign), class: 'btn-primary -mint btn-next' %>
    <% elsif f.object.approved? %>
      <%= link_to 'Dein Projekt wurde freigeschalten', edit_next_steps_crowd_campaign_path(@crowd_campaign), class: 'btn-primary -mint btn-next' %>
    <% else %>
      <%= f.button 'Projekt zur Freigabe einreichen', data: {confirm: 'Klicke auf "OK" um dein Projekt jetzt zur Freigabe einzureichen.',  disable_with: 'speichert...' }, class: 'btn-primary btn-next', name: "submit_for_approve", value: 'approve' %>
    <% end %>
  </div>

  <% end %>

</section>
