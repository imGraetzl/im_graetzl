(function() {
  <% if @comment.persisted? %>
    var $commentForm = $("[data-behavior='comment-form-<%= @crowd_campaign_post.id %>']");
    $commentForm.before("<%= j render @comment %>");
    $commentForm.replaceWith("<%= j render 'crowd_campaigns/crowd_campaign_posts/comment_form', post: @crowd_campaign_post %>");
    APP.components.stream.initCommentForm();
    APP.components.stream.initLinkify($(<%= @comment.target_url_params %>).find('.txtlinky'));
    APP.controllers.application.jBoxGallery();
  <% else %>
    alert('<%= @comment.errors.full_messages.first %>');
  <% end %>
})();
