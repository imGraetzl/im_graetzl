console.log('add comment');
var formID = "<%= j(@form_id.to_s) %>";
$('div#' + formID + '.comment-form').before("<%= j render 'comments/comment', comment: @comment %>");
$('div#' + formID + '.comment-form').replaceWith("<%= j render 'comments/form', type: @commentable.class, id: @commentable.id %>");