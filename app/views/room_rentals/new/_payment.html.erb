<div class="payment">

  <div class="hl-subtext">
    <h1>Wähle deine Zahlungsart</h1>
    <p>
      Wir buchen erst Geld ab, wenn deine Anfrage vom Vermieter bestätigt wurde und der Deal zustande kommt.
    </p>
  </div>

  <div class="paymentMethods">
    <% RoomRental::PAYMENT_METHODS.each do |payment_method| %>
      <div class="input-radio -<%= payment_method %>">
        <%= radio_button_tag :payment_method, payment_method, payment_method == params[:payment_method] %>
        <%= label_tag :"payment_method_#{payment_method}", payment_method_label(payment_method) %>
      </div>
    <% end %>
  </div>

  <div class="payment-method-container card-container">
    <%= render 'room_rentals/new/payment_credit_card' %>
  </div>

  <div class="payment-method-container klarna-container">
    <%= render 'room_rentals/new/payment_klarna' %>
  </div>

  <div class="payment-method-container eps-container">
    <%= render 'room_rentals/new/payment_eps' %>
  </div>

  <%= form_tag summary_room_rentals_url, method: :get, class: 'next-step-form' do %>
    <%= hidden_field_tag :room_offer_id, params[:room_offer_id] %>
    <%= hidden_field_tag :renter_company, params[:renter_company] %>
    <%= hidden_field_tag :renter_name, params[:renter_name] %>
    <%= hidden_field_tag :renter_address, params[:renter_address] %>
    <%= hidden_field_tag :renter_zip, params[:renter_zip] %>
    <%= hidden_field_tag :renter_city, params[:renter_city] %>
    <%= hidden_field_tag :payment_method, params[:payment_method] %>
    <%= hidden_field_tag :stripe_payment_intent_id %>
    <%= hidden_field_tag :stripe_source_id, params[:source] %>
  <% end %>

</div>
